package com.company.models;

public class Administrator extends Person {

    public Administrator(int id, String firstName, String lastName, String username, Enum role) {
        super(id, firstName, lastName, username, role);
    }
}
