package com.company.models;

public class Student extends Person {

    public Student(int id, String firstName, String lastName, String username, Enum role) {
        super(id, firstName, lastName, username, role);
    }
}
